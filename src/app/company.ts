export class Company {
  id: number;
  name: string;
  status: string;  // (e.g., researching, pending approval, approved, declined)
  companyInfo: string;
  keyContacts: string;
  financialPerformance: string;
}
